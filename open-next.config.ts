// Minimal OpenNext config — uses the default Cloudflare preset.
// See https://opennext.js.org/cloudflare for tuning knobs.
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig();
